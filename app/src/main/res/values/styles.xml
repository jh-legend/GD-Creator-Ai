<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.GDCreator" />

    <!-- Button Styles -->
    <style name="PrimaryButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@color/user_message_bubble</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:cornerRadius">16dp</item>
    </style>

    <style name="SecondaryButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/user_message_bubble</item>
        <item name="strokeColor">@color/user_message_bubble</item>
        <item name="android:cornerRadius">16dp</item>
    </style>

    <!-- Text Styles -->
    <style name="TitleText" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="BodyText" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">#B0B0B0</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Card View Style -->
    <style name="AppCardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/input_field_background</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
    </style>

    <!-- EditText Style -->
    <style name="AppEditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_background_dark</item>
        <item name="android:textColorHint">?attr/colorOnSurfaceVariant</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
    </style>

    <style name="InputThemeStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:textColor">?attr/colorOnSurface</item>
        <item name="android:textColorHint">?attr/colorOnSurfaceVariant</item>
        <item name="boxStrokeColor">?attr/colorPrimary</item>
        <item name="boxBackgroundColor">?attr/colorSurface</item>
    </style>

</resources>
